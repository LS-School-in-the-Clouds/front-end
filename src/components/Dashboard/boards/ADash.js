//Functional imports
import react, { useState } from 'react';

//Component Imports

export const ADash = ({props}) => {
    return(
        <>
        <h1>I'm for admins!</h1>
        </>
    );
}

export default ADash;